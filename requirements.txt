pyserial>=3.5
fastapi>=0.110
uvicorn>=0.29
# uvicorn ships no WebSocket implementation of its own; without one the
# dashboard's /ws and /ws/status connections fail and the page never fills.
websockets>=12
